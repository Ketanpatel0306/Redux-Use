import React, { useContext } from "react";
export const CartDetail = () => {
  return (
    <div>
      hello CartDetail
      {/* {cart.map((item, index) => {
        return (
          <CardHome
            item={item}
            cart={cart}
            setCart={setCart}
            key={index + "cartDetail"}
          />
        );
      })} */}
    </div>
  );
};
